"""Example healthcare agents — demonstrates dynamic-model-router with Google ADK.

These are NOT part of the package. They are example agents showing how to
wire the router callback into ADK LlmAgents for healthcare workloads.

Install requirements:
    pip install 'dynamic-model-router[adk,ml]'

Run via ADK web (recommended for testing):
    adk web examples/adk_healthcare

Or the simple runner (one task list):
    python -m examples.adk_healthcare.run
"""
