"""Layer 3 ML package — training scripts, embedding encoder, runtime utilities.

Stage 2: frozen `all-MiniLM-L6-v2` + sklearn MLP heads (this stage)
Stage 3: fine-tuned DistilBERT dual-head (future)
"""
