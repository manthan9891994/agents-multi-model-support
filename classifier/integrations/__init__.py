"""Integrations with agent frameworks (ADK, CrewAI, LangChain, ...).

Each integration is a thin adapter that wires the dynamic-model-router
into a specific framework. Most are optional — install the relevant extra:

    pip install 'dynamic-model-router[adk]'
    pip install 'dynamic-model-router[anthropic]'
"""
